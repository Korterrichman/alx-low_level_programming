#include "main.h"

/**
 * add - function that adds two integers and returns the result
 *
 * @a: formal parameter that receives the first value
 * @b: formal parameter that receives the second value
 *
 * Return: the addition of the integers is returned to the function
 */

int add(int a, int b)
{
	int s = a + b;

	return (s);
}
